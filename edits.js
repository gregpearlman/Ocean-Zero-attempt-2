// edits.js — committed overlay of in-browser edits (auto-written by save-edits).
// Hand-edit with care; the in-app editor rewrites this whole file on each save.
// NEVER let a code push revert this file — see the pre-push hook + docs/07.
window.SITE_EDITS = {};
